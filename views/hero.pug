extends ./layout/main-layout.pug

block header
	include ./layout/head.pug
	script(src="/assets/scripts/script-hero.js" type="text/javascript" defer="")
	script(type="text/javascript").
		var hero="!{currentSession.hero}"
		var nick="!{profile.nick}"
		var platform="!{currentSession.platform}"
		var mode="!{currentSession.mode}"

block phc
	include ./layout/phc.pug
	
block main-stats
	include ./hero/mainstats.pug

	
block bottom
	h3 Recent Play Sessions
	.sessionContainer
		include ./hero/sessionstats.pug
		include ./hero/miscstats.pug
	

	

	